## Get started
On the /backend directory
1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
    node app.js
   ```

On the /frontend directory
1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
    npx expo start --clear
   ```
